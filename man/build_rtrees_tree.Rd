% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylogenetic_diversity_files.R
\name{build_rtrees_tree}
\alias{build_rtrees_tree}
\title{Build one rtrees phylogeny}
\usage{
build_rtrees_tree(
  species,
  group,
  tree = NULL,
  tree_index = 1L,
  scenario = c("at_basal_node", "random_below_basal"),
  show_grafted = FALSE,
  tree_by_user = NULL,
  progress = "none",
  ...,
  .tree_builder = NULL
)
}
\arguments{
\item{species}{A character vector of species names, or a data frame with
case-insensitive \code{species}, \code{genus}, and \code{family} columns. Optional
\code{close_sp} and \code{close_genus} columns are retained.}

\item{group}{One group returned by \code{\link[=classify_rtrees_group]{classify_rtrees_group()}}.}

\item{tree}{Optional user-supplied \code{phylo} or \code{multiPhylo}. When omitted,
the default megatree for \code{group} is loaded from \code{megatrees}.}

\item{tree_index}{One-based tree to select from a posterior \code{multiPhylo}.
Default \code{1L}.}

\item{scenario}{Grafting scenario passed to \code{rtrees::get_tree()}.}

\item{show_grafted}{Whether \code{rtrees} should append stars to grafted tip
labels. Default \code{FALSE}; graft status remains available in the returned
tree's \code{graft_status} table.}

\item{tree_by_user}{Whether \code{tree} is user supplied. \code{NULL} detects this
from whether \code{tree} was supplied.}

\item{progress}{Progress-bar style passed as \code{.progress} to \code{rtrees}.}

\item{...}{Additional named arguments passed to \code{rtrees::get_tree()}.}

\item{.tree_builder}{Optional replacement for \code{rtrees::get_tree()}, used
for controlled testing.}
}
\value{
One \code{phylo} object, with requested species, group, and tree index
recorded as attributes.
}
\description{
Builds a single group-specific phylogeny with \code{rtrees::get_tree()}. When a
taxon uses a posterior collection, \code{tree_index} selects one megatree before
species are grafted. This avoids accidentally constructing all posterior
trees during a pilot run and makes the selected tree reproducible.
}
\details{
A taxonomic data frame with \code{species}, \code{genus}, and \code{family} is recommended,
especially for bees, butterflies, and species absent from a megatree.
Species names are converted to the underscore format required by \code{rtrees}.
}
