% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/range_rarity_files.R
\name{calc_range_weights_files}
\alias{calc_range_weights_files}
\title{Calculate baseline range-size weights from raster files}
\usage{
calc_range_weights_files(
  paths,
  template,
  unit = c("km2", "cells"),
  align = c("error", "near"),
  verbose = TRUE
)
}
\arguments{
\item{paths}{Named character vector of single-layer binary raster files.
Names must be species names. The named vector returned by
\code{\link[=select_projection_paths]{select_projection_paths()}} is suitable.}

\item{template}{A \code{terra::SpatRaster} or raster file path vector defining
the common output geometry and valid domain. A cell is included only when
every template layer is non-missing.}

\item{unit}{Range-size unit: \code{"km2"} (recommended for longitude/latitude
rasters) or \code{"cells"} for an equal-area grid.}

\item{align}{Geometry handling. \code{"error"} requires every binary raster to
match \code{template}. \code{"near"} aligns mismatched binary rasters to the
template with nearest-neighbour resampling.}

\item{verbose}{Whether to report progress. Default \code{TRUE}.}
}
\value{
A data frame containing \code{species}, \code{range_size}, \code{weight},
\code{range_unit}, and \code{binary_path}. A species with no occupied baseline cells
receives weight zero.
}
\description{
Reads one binary species raster at a time and calculates its baseline range
size inside a common comparison domain. This avoids constructing a complete
cells-by-species raster stack.
}
\details{
Range sizes should normally be calculated once from current-condition
projections and reused unchanged for all scenarios. With \code{unit = "km2"},
the range size of species \eqn{i} is

\deqn{A_i = \sum_j B_{ij} a_j,}

where \eqn{B_{ij}} is binary presence and \eqn{a_j} is cell area in square
kilometres. The corresponding weight is \eqn{w_i = 1 / A_i}.
}
