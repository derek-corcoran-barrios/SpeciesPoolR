% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SDM.R
\name{load_one_scenario}
\alias{load_one_scenario}
\title{Load a Single User-Supplied Scenario Raster}
\usage{
load_one_scenario(path, crop_to = NULL, categorical = NULL)
}
\arguments{
\item{path}{A path (or vector of paths, for a scenario made of several
layers) for one scenario.}

\item{crop_to}{Optional: a study-area boundary to crop and mask to -- a
path to a vector file, or an already-loaded \code{SpatVector}.}

\item{categorical}{Optional character vector naming which layer(s) are
categorical (e.g. land use). See \code{\link[=SampleEnv]{SampleEnv()}}.}
}
\value{
A single \code{SpatRaster} for this one scenario.
}
\description{
Loads and (optionally) crops/masks one scenario's raster file(s). This is
the per-branch counterpart to \code{\link[=load_scenario_collection]{load_scenario_collection()}} -- use this
one (branched with \code{pattern = map()} over a spec of scenarios) inside a
\code{targets} pipeline via \code{geotargets::tar_terra_rast()}, not
\code{tar_terra_sprc()}: \code{tar_terra_sprc()} has no \code{preserve_metadata}
argument, so a categorical layer's real category labels (e.g. "Forest",
"Agriculture") can silently come back as plain numeric codes after the
storage round trip, which is exactly the kind of mismatch that breaks
prediction against a model trained on the real labels. \code{tar_terra_rast()}
does support \code{preserve_metadata}, hence loading scenarios one at a time.
}
