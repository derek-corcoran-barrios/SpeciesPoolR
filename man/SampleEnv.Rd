% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SDM.R
\name{SampleEnv}
\alias{SampleEnv}
\title{Sample Environmental Data for Species Presences or Background Locations}
\usage{
SampleEnv(DF, file, categorical = NULL, type = "pres", n_bg = 10000)
}
\arguments{
\item{DF}{A data frame with columns \code{species}, \code{decimalLongitude}, and
\code{decimalLatitude}.}

\item{file}{A path (or vector of paths) to raster file(s) with the
environmental layers. Passed to \code{terra::rast()}, which stacks multiple
files into one multi-layer \code{SpatRaster} automatically.}

\item{categorical}{Optional character vector naming which layer(s) in the
stack are categorical (e.g. land use). Layers not listed here are
treated as continuous. Layers already stored as factors in the raster
itself don't need to be listed.}

\item{type}{Either \code{"pres"} (sample at presence locations) or \code{"bg"}
(sample background locations). Defaults to \code{"pres"}.}

\item{n_bg}{Number of background points to sample when \code{type = "bg"}.
Default 10000.}
}
\value{
A data frame with one column per environmental layer (factor for
categorical layers, numeric for continuous ones), plus \code{species} and
\code{Pres} (1 for presence rows, 0 for background rows).
}
\description{
Samples one or more environmental layers (categorical and/or continuous)
either at species presence locations or at background locations drawn
from within an expanded convex hull around those presences.
}
