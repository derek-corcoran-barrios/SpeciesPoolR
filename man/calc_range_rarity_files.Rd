% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/range_rarity_files.R
\name{calc_range_rarity_files}
\alias{calc_range_rarity_files}
\title{Calculate file-backed range-size rarity}
\usage{
calc_range_rarity_files(
  paths,
  Weights,
  template,
  filename,
  name = "RangeRarity",
  output = c("density", "cell_contribution"),
  chunk_size = 50L,
  align = c("error", "near"),
  overwrite = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{paths}{Named character vector of single-layer binary raster files,
normally returned by \code{\link[=select_projection_paths]{select_projection_paths()}}.}

\item{Weights}{Baseline weight table returned by
\code{\link[=calc_range_weights_files]{calc_range_weights_files()}} or \code{\link[=calc_range_weights]{calc_range_weights()}}.}

\item{template}{A \code{terra::SpatRaster} or raster file path vector defining
the common geometry and non-missing output domain.}

\item{filename}{Output GeoTIFF path.}

\item{name}{Output raster-layer name. Default \code{"RangeRarity"}.}

\item{output}{Either \code{"density"} or \code{"cell_contribution"}.}

\item{chunk_size}{Maximum number of species rasters opened in a first-level
reduction. Default \code{50L}.}

\item{align}{Geometry handling. See \code{\link[=calc_range_weights_files]{calc_range_weights_files()}}.}

\item{overwrite}{Whether \code{filename} may be replaced. Default \code{TRUE}.}

\item{verbose}{Whether to report reduction progress. Default \code{TRUE}.}
}
\value{
A file-backed, single-layer \code{terra::SpatRaster}.
}
\description{
Calculates range-size rarity from named binary raster files using a bounded
tree reduction. At most \code{chunk_size} species rasters are opened for one
first-level sum, and only partial sums are opened at later levels.
}
\details{
Baseline weights are matched to files by normalized species name, never by
row position. Supply the same baseline \code{Weights} for current conditions and
every scenario.

\code{output = "density"} returns \eqn{\sum_i B_{ij} / A_i}. For kilometre-based
weights, \code{output = "cell_contribution"} multiplies this value by cell area,
so each species contributes a total of one across its baseline range. On an
equal-area cell-count analysis, density and cell contribution are the same.
}
