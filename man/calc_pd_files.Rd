% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylogenetic_diversity_files.R
\name{calc_pd_files}
\alias{calc_pd_files}
\title{Calculate file-backed phylogenetic diversity}
\usage{
calc_pd_files(
  paths,
  Tree,
  template,
  filename,
  name = "PD",
  include_root = FALSE,
  unmatched = c("error", "drop"),
  align = c("error", "near"),
  max_cells_per_block = 20000L,
  overwrite = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{paths}{Named character vector of single-layer binary raster files.
Names must be species names, for example the output of
\code{\link[=select_projection_paths]{select_projection_paths()}}.}

\item{Tree}{One rooted \code{phylo} with finite branch lengths. Legacy
V.PhyloMaker results containing \code{scenario.3} are also accepted.}

\item{template}{A \code{terra::SpatRaster} or raster file path vector defining
the output geometry and common non-missing domain.}

\item{filename}{Output GeoTIFF path.}

\item{name}{Output raster-layer name. Default \code{"PD"}.}

\item{include_root}{Whether to include branches from the assemblage's most
recent common ancestor to the tree root. Default \code{FALSE}.}

\item{unmatched}{How species files absent from the tree are handled.
\code{"error"} is the safe default; \code{"drop"} explicitly excludes them.}

\item{align}{Geometry handling. \code{"error"} requires every binary raster to
match \code{template}; \code{"near"} uses nearest-neighbour alignment.}

\item{max_cells_per_block}{Maximum number of spatial cells read per block.
Memory also scales with the number of species in this tree group.}

\item{overwrite}{Whether \code{filename} may be replaced. Default \code{TRUE}.}

\item{verbose}{Whether to report block progress. Default \code{TRUE}.}
}
\value{
A file-backed, single-layer \code{terra::SpatRaster} in the branch-length
units of \code{Tree}.
}
\description{
Calculates Faith's phylogenetic diversity from named, single-layer binary
raster files. Raster values are read in spatial blocks and converted to a
sparse presence matrix. Tree edges are represented once as a sparse
species-by-edge matrix, so the complete study-area cells-by-species matrix
is never materialized.
}
\details{
Missing values in a species raster are treated as absence inside the common
template domain, matching the existing richness reduction. Cells outside
that domain remain \code{NA} in the output.

With the default \code{include_root = FALSE}, an edge contributes to a cell only
when at least one, but not all, species present in that cell descend from
the edge. This is the minimal subtree connecting the present species and is
equivalent to \code{picante::pd(..., include.root = FALSE)} for assemblages with
at least two species. Consistent with \code{\link[=calc_pd_raster]{calc_pd_raster()}}, empty and
single-species cells are recorded as PD zero rather than \code{NA}.
}
