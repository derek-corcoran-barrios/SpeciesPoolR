% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{make_landuse_scenarios}
\alias{make_landuse_scenarios}
\title{Build Land-Use Scenario Rasters by Recoding a Category (Convenience Helper)}
\usage{
make_landuse_scenarios(file, old_value, new_value, name)
}
\arguments{
\item{file}{Path to the baseline land-use raster.}

\item{old_value}{The raw category code to replace (e.g. Agriculture).}

\item{new_value}{Numeric vector of replacement category codes, one per
scenario.}

\item{name}{Character vector of scenario names, same length as
\code{new_value}, used as each scenario's layer name (so \code{categorical}
matching still works downstream).}
}
\value{
A \code{SpatRasterCollection}, one member per scenario.
}
\description{
A convenience for the simple case: mechanically recoding one land-use
category into alternatives on a raster that's already at the right
extent (e.g. testing/demo purposes). For a real analysis with
externally-produced scenarios or a larger source extent that needs
cropping to a study area, use \code{\link[=load_scenario_collection]{load_scenario_collection()}} instead.
}
