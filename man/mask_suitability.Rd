% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/final_presence.R
\name{mask_suitability}
\alias{mask_suitability}
\title{Mask Suitability by a Dispersal Buffer, Preserving True No-Data}
\usage{
mask_suitability(Model, Mask)
}
\arguments{
\item{Model}{A single-layer \code{SpatRaster} of predicted suitability for
one species (as produced by \code{\link[=PredictSuitability]{PredictSuitability()}}).}

\item{Mask}{A \code{SpatVector} -- the species' dispersal buffer, as produced
by \code{\link[=make_buffer]{make_buffer()}}.}
}
\value{
A \code{SpatRaster}, same extent and resolution as \code{Model}: original
suitability values inside the buffer, 0 outside the buffer (wherever
\code{Model} had real data), \code{NA} wherever \code{Model} itself was \code{NA}.
}
\description{
Sets suitability to 0 outside the buffer -- not \code{NA} -- so that a later
sum across species (for richness) treats "not within reach of this
species" as a real zero rather than a missing value. Genuine no-data
cells (e.g. ocean, outside the study area) stay \code{NA}:
\code{terra::mask(..., updatevalue = 0)} alone would zero those out too,
since it only checks whether a cell falls inside the mask geometry, not
whether the input itself was already \code{NA} there -- so this re-masks
against the original \code{Model} afterward to restore true \code{NA} wherever the
environment itself had no data.
}
