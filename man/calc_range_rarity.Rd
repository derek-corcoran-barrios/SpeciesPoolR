% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/range_rarity.R
\name{calc_range_rarity}
\alias{calc_range_rarity}
\title{Calculate Range-Size Rarity / Irreplaceability}
\usage{
calc_range_rarity(Binary, Weights, name = "RangeRarity")
}
\arguments{
\item{Binary}{A multi-layer binary (0/1) \code{SpatRaster}, one layer per
species, or a list of single-layer \code{SpatRaster} objects.}

\item{Weights}{A data frame produced by \code{\link[=calc_range_weights]{calc_range_weights()}} containing
columns \code{species} and \code{weight}.}

\item{name}{Character name for the output raster layer.
Default \code{"RangeRarity"}.}
}
\value{
A single-layer \code{SpatRaster} containing range-size rarity values.
Cells that are \code{NA} for every species remain \code{NA}.
}
\description{
Calculates a cell-level range-size rarity score as the sum of the baseline
inverse-range weights of all species predicted present in each cell:
}
\details{
\deqn{RSR_j = \sum_i B_{ij} w_i}

where \eqn{B_{ij}} is 1 when species \eqn{i} is predicted present in cell
\eqn{j}, and \eqn{w_i = 1/A_i} is the inverse baseline range size produced
by \code{\link[=calc_range_weights]{calc_range_weights()}}.

This is also commonly interpreted as weighted endemism or a simple
irreplaceability score. Cells containing geographically restricted species
receive larger values than cells containing only widespread species.

Species weights are matched to raster layers BY NAME, not by position.
Species missing from the weight table cause an error. Species with baseline
weight 0 contribute 0 to the score.
}
