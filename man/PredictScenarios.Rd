% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{PredictScenarios}
\alias{PredictScenarios}
\title{Predict Suitability Across a Collection of Land-Use Scenarios}
\usage{
PredictScenarios(Models, Scenarios, categorical = NULL)
}
\arguments{
\item{Models}{A named list of fitted \code{maxnet} models, one per species
(the \emph{combined} list across all species, not a single-species branch).}

\item{Scenarios}{A \code{SpatRasterCollection}, as produced by
\code{\link[=load_scenario_collection]{load_scenario_collection()}} or \code{\link[=make_landuse_scenarios]{make_landuse_scenarios()}}.}

\item{categorical}{Optional character vector naming which layer(s) are
categorical. See \code{\link[=SampleEnv]{SampleEnv()}}.}
}
\value{
A \code{SpatRasterCollection}, one member per scenario, each member a
multi-layer \code{SpatRaster} (one layer per species) of predicted
suitability -- directly comparable to a baseline stack built the same
way from \code{\link[=PredictSuitability]{PredictSuitability()}}.
}
\description{
Applies \code{\link[=PredictSuitability]{PredictSuitability()}} to every member of a scenario
\code{SpatRasterCollection}, using the same already-fitted models each time
(see \code{\link[=FitSpeciesModels]{FitSpeciesModels()}}) -- no refitting per scenario.
}
