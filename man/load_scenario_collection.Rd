% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{load_scenario_collection}
\alias{load_scenario_collection}
\title{Load User-Supplied Scenario Rasters (All at Once, for Interactive Use)}
\usage{
load_scenario_collection(files, crop_to = NULL, categorical = NULL)
}
\arguments{
\item{files}{A named list. Each element is a path (or vector of paths,
for a scenario made of several layers) for one scenario. The list
names become the scenario names.}

\item{crop_to}{Optional: a study-area boundary to crop and mask every
scenario to -- a path to a vector file, or an already-loaded
\code{SpatVector}. Use this when your scenario rasters cover a larger area
than you actually want to study (e.g. all of Europe, but you only care
about Denmark).}

\item{categorical}{Optional character vector naming which layer(s) are
categorical (e.g. land use). See \code{\link[=SampleEnv]{SampleEnv()}}.}
}
\value{
A \code{SpatRasterCollection}, one member per scenario, named as in
\code{files}.
}
\description{
The multi-scenario convenience version of \code{\link[=load_one_scenario]{load_one_scenario()}}: loads
every scenario in one call and packages them into a \code{SpatRasterCollection}
for easy interactive inspection (e.g. \code{terra::plot(Scenarios)}). Fine to
use this directly outside a pipeline. Inside a \code{targets} pipeline, prefer
branching with \code{\link[=load_one_scenario]{load_one_scenario()}} via \code{geotargets::tar_terra_rast()}
instead of wrapping this in \code{tar_terra_sprc()} -- see \code{\link[=load_one_scenario]{load_one_scenario()}}
for why.
}
