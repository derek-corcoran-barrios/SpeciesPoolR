% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_pd.R
\name{calc_pd}
\alias{calc_pd}
\title{Calculate Phylogenetic Diversity}
\usage{
calc_pd(Fin, Tree)
}
\arguments{
\item{Fin}{A data frame or data table containing species presence data. The data should include columns for \code{species}, \code{cell}, and \code{Landuse}.}

\item{Tree}{A phylogenetic tree object, typically generated by the \code{V.PhyloMaker::phylo.maker} function. The tree must contain a component named \code{scenario.3} with the tip labels.}
}
\value{
A data table containing the calculated phylogenetic diversity (PD) for each cell, along with the associated \code{cell} and \code{Landuse}.
}
\description{
This function calculates the phylogenetic diversity (PD) for a given dataset and phylogenetic tree.
}
\examples{
\dontrun{
  # Example usage:
  Fin <- data.table(cell = c(1, 1, 2, 2), species = c("species_a", "species_b", "species_a", "species_c"), Landuse = c("forest", "forest", "grassland", "grassland"))
  Tree <- V.PhyloMaker::phylo.maker(...)  # Assume this generates a valid tree object
  pd_results <- calc_pd(Fin, Tree)
  print(pd_results)
}
}
