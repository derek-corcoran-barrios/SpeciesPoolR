% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diversity.R
\name{calc_pd_raster}
\alias{calc_pd_raster}
\title{Calculate Phylogenetic Diversity, Cell by Cell}
\usage{
calc_pd_raster(Binary, Tree)
}
\arguments{
\item{Binary}{A multi-layer binary (0/1) \code{SpatRaster}, one layer per
species, layer-named by species.}

\item{Tree}{A phylogenetic tree, as produced by \code{\link[=generate_tree]{generate_tree()}}.
Uses the \code{scenario.3} tree, matching the pre-\code{geotargets} version.}
}
\value{
A single-layer \code{SpatRaster} of PD values, \code{NA} wherever \code{Binary}
was \code{NA}. Cells with no species matching the tree get PD 0.
}
\description{
Computes \code{picante::pd()} once for every non-\code{NA} cell in a binary
presence/absence stack, rather than the pre-\code{geotargets} version's
per-land-use-category loop over a long-format table.
}
