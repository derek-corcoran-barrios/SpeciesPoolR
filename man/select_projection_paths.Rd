% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projection_workflow.R
\name{select_projection_paths}
\alias{select_projection_paths}
\title{Validate and select binary projection paths}
\usage{
select_projection_paths(
  projection_summary,
  model_audit,
  scenario,
  exclusions = NULL,
  require_files = TRUE
)
}
\arguments{
\item{projection_summary}{Projection summary table containing \code{scenario},
\code{species}, and \code{binary_path}.}

\item{model_audit}{Model audit from \code{\link[=summarise_model_branches]{summarise_model_branches()}}.}

\item{scenario}{Scenario to select.}

\item{exclusions}{Optional data frame with \code{species}, or a character vector
of species approved for terminal exclusion.}

\item{require_files}{If \code{TRUE} (default), verify that every selected path
exists.}
}
\value{
A named character vector of binary raster paths, with species names
as names.
}
\description{
Selects exactly one binary raster for every successfully fitted,
non-excluded species in one scenario. Missing summaries, duplicated species,
missing paths, and missing files are treated as unresolved errors. The
returned vector is ordered to match the model audit, making downstream
reductions deterministic.
}
\details{
\code{exclusions} may be supplied manually for recovery of an older cached run,
but new pipelines should derive it with \code{\link[=projection_exclusions]{projection_exclusions()}} from
standardized status markers.
}
